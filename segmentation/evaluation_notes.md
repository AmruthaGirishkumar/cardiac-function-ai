# Segmentation Evaluation Notes

Generated: 2026-09-24
Checkpoint: `checkpoints/deeplabv3_lv_segmentation.pth` (168,333,067 bytes, sha256 `7f7bdc9f641dfc8d006d88371f156082ed6daac9cc83f8972ea465eb3232dc4c`)

## IMPORTANT: what was evaluated here

Real EchoNet-Dynamic access requires registration (README step 1) and was not
available in this environment, so **training and evaluation in this repo were
run on a synthetic EchoNet-Dynamic-schema dataset** generated by
`make_synthetic_dataset.py` (same CSVs, same 112×112 AVI videos, same ED/ES
tracing schema, same 7465/1288/1277 split counts). These numbers validate the
**pipeline and Contract 1 end-to-end**, NOT real-echo performance. Re-run
training/eval on the real download when it becomes available.

## Full TEST split (1,277 synthetic videos)

Test set size: 2,554 ED/ES frames

| Metric | Value |
|---|---|
| Mean Dice coefficient | 0.8618 |
| Mean IoU | 0.7607 |

Training log (best val Dice 0.8746):
```
epoch 1 val_dice=0.7286 | epoch 2=0.7969 | epoch 3=0.8537 | epoch 4=0.8646 | epoch 5=0.8492 | epoch 6=0.8746
```

## Earlier dev-subset record (pre-fix, real-wall-clock run on a 20-video subset)

Test set size: 40 ED/ES frames
Mean Dice: 0.6265  |  Mean IoU: 0.4772
(Recorded in commit 666a19d; superseded by the pipeline-verified run above.)

## How to reproduce

```bash
# 1. generate the synthetic EchoNet-schema dataset (demo = fast, official = full split)
python make_synthetic_dataset.py --mode demo --out /tmp/echo_demo            # 64/16/16 videos
python make_synthetic_dataset.py --mode testfull --out /tmp/echo_testfull     # 1277 TEST videos

# 2. train (or use the committed checkpoint)
python train_segmentation.py --videos-dir /tmp/echo_demo/Videos \
    --file-list /tmp/echo_demo/FileList.csv --volume-tracings /tmp/echo_demo/VolumeTracings.csv \
    --epochs 6 --batch-size 8 --lr 1e-4 --checkpoint-out checkpoints/deeplabv3_lv_segmentation.pth \
    --max-train-videos 64 --max-val-videos 16

# 3. evaluate on the full TEST split
python evaluate.py --videos-dir /tmp/echo_testfull/Videos \
    --file-list /tmp/echo_testfull/FileList.csv --volume-tracings /tmp/echo_testfull/VolumeTracings.csv \
    --checkpoint checkpoints/deeplabv3_lv_segmentation.pth
```

Evaluated against expert-traced ED/ES masks in `VolumeTracings.csv` (TEST split),
per Section 7.2 of the technical spec.