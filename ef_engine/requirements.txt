# Core: ef_module.py (Contract 2) runs on NumPy alone.
numpy>=1.24.0

# validate_ef.py -- Validation A (ground-truth masks from VolumeTracings.csv):
pandas>=2.0
scikit-image>=0.21        # polygon rasterization via segmentation/dataset.tracing_to_mask

# validate_ef.py -- Validation B (predicted masks via Contract 1 segment_video):
torch>=2.0
torchvision>=0.15
opencv-python-headless>=4.8
